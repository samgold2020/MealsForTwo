import React, { useState } from 'react';

const Recipe = ({ recipes, setRecipes }) => {

    return (
        <div>
            
            <p>Single Recipe</p>
        </div>
    );
};

export default Recipe;